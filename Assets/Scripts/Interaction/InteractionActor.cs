namespace PandaCafe.Interaction
{
    // Defines who is performing an interaction in the system.
    // Used to distinguish behavior between different actor types.
   public enum InteractionActor
    {
        Waiter,
        Guest
    } 
}

