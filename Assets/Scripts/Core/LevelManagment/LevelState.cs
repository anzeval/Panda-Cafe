// Defines active level states.
// Used by level manager and UI controller.
// Separates play, pause, win, and loss states.
namespace PandaCafe.Core.LevelManagment
{
    public enum LevelState 
    {
        Playing,
        Pause,
        Win,
        Lost
    }   
}
